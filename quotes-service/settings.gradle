pluginManagement {
  repositories {
    maven { url 'https://repo.spring.io/release' }
    maven { url 'https://repo.spring.io/snapshot' }
    gradlePluginPortal()
  }
}
rootProject.name = 'reactive-quote-service'
